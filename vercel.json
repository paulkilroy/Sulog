{
  "buildCommand": "npm run build && rm -rf dist && mkdir dist && cp index.html dist/index.html && cp docs/preview/verify.html dist/verify.html && cp -r docs/preview/verify dist/verify",
  "outputDirectory": "dist",
  "framework": null
}